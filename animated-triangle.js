const canvasSketch = require('canvas-sketch');

const settings = {
  duration: 3,
  dimension: [640, 640],
  scaleToView: true,
  playbackRate: 'throttle',
  animate: true,
  fps: 24
}

const sketch = () => {

}

canvasSketch(sketch, settings);
